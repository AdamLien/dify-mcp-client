version: 0.0.1
type: plugin
author: 3dify
name: mcp_client
label:
  en_US: mcp_client
  ja_JP: mcp_client
  zh_Hans: mcp_client
  pt_BR: mcp_client
description:
  en_US: Claude MCP Client as Dify plugin. This maintains 1:1 connections with MCP servers. By default, list of {tool, resource, prompt} on the server is passed to LLM. Dify is regarded as Host (not MCP server).
  ja_JP: Claude MCP Client as Dify plugin. This maintains 1:1 connections with MCP servers. By default, list of {tool, resource, prompt} on the server is passed to LLM. Dify is regarded as Host (not MCP server).
  zh_Hans: Claude MCP Client as Dify plugin. This maintains 1:1 connections with MCP servers. By default, list of {tool, resource, prompt} on the server is passed to LLM. Dify is regarded as Host (not MCP server).
  pt_BR: Claude MCP Client as Dify plugin. This maintains 1:1 connections with MCP servers. By default, list of {tool, resource, prompt} on the server is passed to LLM. Dify is regarded as Host (not MCP server).
icon: icon.svg
resource:
  memory: 268435456
  permission:
    tool:
      enabled: true
    model:
      enabled: true
      llm: true
      text_embedding: false
      rerank: false
      tts: false
      speech2text: false
      moderation: false
    endpoint:
      enabled: true
    app:
      enabled: true
    storage:
      enabled: true
      size: 1048576
plugins:
  agent_strategies:
    - provider/agent.yaml
meta:
  version: 0.0.1
  arch:
    - amd64
    - arm64
  runner:
    language: python
    version: "3.12"
    entrypoint: main
created_at: 2025-03-04T16:50:45.7131549+09:00
privacy: PRIVACY.md
verified: false
